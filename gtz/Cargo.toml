[package]
name = "get-time-zone"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = { version = "0.4.44", default-features = false, features = ["clock", "std", "now"] }
chrono-tz = { version = "0.10.4", default-features = false }
